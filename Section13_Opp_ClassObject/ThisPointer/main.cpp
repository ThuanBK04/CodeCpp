/*

This Pointer
- this is a reserved keyword
- Contains the address of the object - so it's a pointer to object
- Can only be used in class scope
- All member access is does via the this pointer
- Can be used by the programmer
+ To access data member and methods
+ To determine if two objects are the same
+ Can be deferenced (*this) to yield the current object

*/

#include <iostream>

using namespace std;

int main()
{
	cout << "Default Template!" << endl; 
	return 0;
}
